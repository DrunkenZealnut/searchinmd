# 의미 출현 등급 결합 계획

> Version: 1.0.0 | Date: 2026-09-09 | Status: Approved
> Level: Starter

## 1. 목적

`semantic_keyword_recount_20260909.xlsx`의 NCS·교과서 의미 출현마다 기존 페이지 등급(등급1~3)을 결합하고, 두 대시보드를 동일한 교과서 분석 흐름과 출현건수 기준으로 통일한다.

## 2. 범위

### 포함

- 기존 페이지 등급을 문서·페이지 키로 의미 출현에 상속
- 새로 검출된 페이지는 기존 기준선 판정 함수로 등급 계산
- 페이지 마커가 없는 NCS 출현은 `등급 미확정`으로 분리
- 재검산 Excel 상세·파일별·요약 시트에 등급과 판정 출처 추가
- NCS·교과서 페이지를 등급 KPI → 전체 분포 → 키워드 상세 → 문서/영역 → 시사점 → 비교 → 권고 순으로 통일
- 등급 분포와 비교값의 분모를 의미 출현건수로 통일

### 제외

- 기존 원본 Excel 직접 수정
- 등급 정의나 임계값 변경
- 페이지가 없는 출현에 문서 단위 등급을 강제 부여
- 보류·제외 표현을 출현건수에 포함

## 3. 성공 기준

- [x] 모든 페이지 확인 가능 출현에 등급1~3, 등급명, 사유, 출처가 있다.
- [x] 페이지 미확정 출현은 별도 상태로 보존되고 등급 비율 분모에서 제외된다.
- [x] 등급별 출현건수 합이 등급 확정 출현건수와 일치한다.
- [x] 기존 페이지는 기존 등급을 보존하고 신규 페이지만 재판정한다.
- [x] NCS와 교과서 대시보드는 동일한 섹션 순서와 등급1~3 표현을 쓴다.
- [x] 기존 및 신규 회귀 테스트가 모두 통과한다.

## 4. 위험과 대응

| 위험 | 대응 |
|---|---|
| NCS 문서명과 현재 Markdown 파일명 형식 차이 | NCS 능력단위 코드를 우선 키로 삼고 Unicode·타임스탬프·확장자를 정규화 |
| 교과서 원본의 반대 등급 코드 | 기존 교과서 Excel을 읽을 때 통일 등급 1→3으로 재매핑 |
| 신규 페이지 등급 누락 | Markdown 페이지 전체에 `regrade.grade_page()` 기준선 적용 |
| 페이지 없는 대형 문서가 분포를 왜곡 | 출현 총계에는 유지하되 `등급 미확정`으로 별도 표시 |
| 원본과 산출물 혼동 | 원본은 읽기 전용으로 두고 재검산 Excel만 재생성 |

## 5. 진행 순서

| 단계 | 내용 | 상태 |
|---|---|---|
| Plan | 범위와 성공 기준 확정 | 완료 |
| Design | 결합 키·등급 출처·출력 구조 정의 | 완료 |
| Do | 테스트 우선 구현 및 산출물/대시보드 갱신 | 완료 |
| Check | 전체 테스트·수치 정합성·화면 렌더 검증 | 완료 |
| Report | 결과와 한계 문서화 | 완료 |

## 6. 입력 자료

- `data/semantic_keyword_recount_20260909.xlsx`
- `data/ncs_keywords_in_markdown_results_20260402_재판정_20260414.xlsx`
- `data/ncs_keywords_in_markdown_results_교과서_results_20260415.xlsx`
