.DS_Store
.bkit/
.claude/
audit.jsonl
uploads/
.gstack/

# 원본 연구 데이터 (67MB) — 공개 저장소에 올리지 않고 로컬 보관
# recount_grades.py 가 data/ 에서 읽어 docs/03-analysis/data/ 로 산출물을 만든다
/data/

# 백업·캐시
.git.bak-*/
*.bak-*
__pycache__/
.gstack-audit/
.vscode/
venv/

# 다운로더 기본 저장 위치 (DOWNLOAD_ROOT 미설정 시)
/downloads/

# 수기 코딩 시트의 **본문** 만 무시한다 — 비공개 상업 교재 전문이 들어간다.
/coding_sheet.json
/coding_sheet.md

# coding_key.json / coding_A.json / coding_B.json 은 **추적한다.**
# 본문이 없다(교재명·쪽·군·카운트와 등급 라벨뿐, 최장 값 38자). 이 셋이 발표된
# κ 0.796 / 일치 88.4% 의 유일한 원자료이고, 없으면 그 수치를 아무도 재현할 수
# 없다. 이전에는 `/coding_*.json` 와일드카드가 교재 본문 사유로 함께 쓸어갔다.


# 코더 API 키 (code_pages.py --provider-env). 절대 커밋하지 않는다.
/.env
/.env.local
# write_atomic 이 강제 종료(SIGKILL) 때 남길 수 있는 반쪽 산출물 — 추적 디렉터리에 커밋되지 않게
/docs/03-analysis/data/*.tmp
